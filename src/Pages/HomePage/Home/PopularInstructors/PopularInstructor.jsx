import { useQuery } from "@tanstack/react-query";

const PopularInstructor = () => {
    // const {} = useQuery
    return (
        <div>
            
        </div>
    );
};

export default PopularInstructor;